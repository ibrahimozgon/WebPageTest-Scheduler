namespace WebPageTest
{
    public enum LocatorType
    {
        XPath,
        CssSelector,
        LinkText,
        Id,
        Name,
        TagName,
        ClassName
    }
    public enum DropdownSelector
    {
        Text = 2,
        Value = 3
    }
}